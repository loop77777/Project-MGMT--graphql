1. initialize graphql schema with our sample data. 
2. run the queries for project and clients.
3. initialize a mongodb database and make the schema.
   
───server    
    ├───config  *database config file*
    ├───models  *mongoose models*
    └───schema  *graphql schema*
-index.js *our main file*
-sampleData.js *sample data that we used before to run and see the graphql queries*

